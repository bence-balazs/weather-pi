version: '3.8'

services:

  db:
    container_name: pg-db
    image: postgres:15.3-alpine3.18
    restart: always
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
